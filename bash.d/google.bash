# The next line updates PATH for the Google Cloud SDK.
[[ -r "$HOME/.local/opt/google-cloud-sdk/path.bash.inc" ]] && source "$HOME/.local/opt/google-cloud-sdk/path.bash.inc"

# The next line enables shell command completion for gcloud.
[[ -r "$HOME/.local/opt/google-cloud-sdk/completion.bash.inc" ]] && source "$HOME/.local/opt/google-cloud-sdk/completion.bash.inc"
